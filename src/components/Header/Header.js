import React from 'react';

const Header = () => {
  return (
    <header style={{ display: 'flex', justifyContent: 'flex-end', padding: '1rem', background: '#F0F0F0' }}>
      <span>Admin account</span>
      {/* 알림 아이콘 컴포넌트를 여기에 삽입 */}
    </header>
  );
};

export default Header;
